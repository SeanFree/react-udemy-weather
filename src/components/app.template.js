import React from 'react';
import { SearchBar, WeatherList } from '../containers';

export default function() {
    return (
        <div>
            <SearchBar />
            <WeatherList />
        </div>
    );
}
